<%= form_with(model: my_wallet, local: true) do |form| %>
  <% if my_wallet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(my_wallet.errors.count, "error") %> prohibited this my_wallet from being saved:</h2>

      <ul>
        <% my_wallet.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :balance %>
    <%= form.text_field :balance %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
